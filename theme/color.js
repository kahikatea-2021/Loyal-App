const tintColorLight = '#8977CE'
const tintColorDark = '#000'

const primary = '#49378E'

export default {
	light: {

		text: '#FCFAF1',
		background: primary,
		primary,
		tint: tintColorLight,
		tabIconDefault: '#ccc',
		tabIconSelected: tintColorLight,
	},
	dark: {

		text: '#FCFAF1',
		background: '#000',
		tint: tintColorDark,
		tabIconDefault: '#ccc',
		tabIconSelected: tintColorDark,
	},
}
